
for each .oscad file in the input directory

	verify that a corresponding baseline .png file esists
	
	generate a proposed version of the .png from the current .oscad files
	
	compare the baseline version to the proposed version
		
		report any comparison conflicts