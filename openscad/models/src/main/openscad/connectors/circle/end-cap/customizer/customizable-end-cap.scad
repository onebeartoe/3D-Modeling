
use <../endcap.scad>

height = 6;
innerRadius = 29;
outerRadius = 33;
endThickness = 3;

endcap(height = height,
				innerRadius = innerRadius,
				outerRadius = outerRadius,
				endThickness = endThickness);
